<!DOCTYPE html>
<html> 
<head> 
<title>SMASH CARE MOVEMENT</title>
</head> 
<body> 
<h1>의사정보수정</h1> 
<% if (dbcheck) { %>
 	<script>
        alert("중요정보를 입력하지 않았습니다.");
	</script>
<% } %>
<form method="post">
           <div>
                <label>의료진 번호</label>
                <input type='text' name="employeeNumber" value="<%= employeeNumber %>"/>
            </div>
            <div>
                <label>계정 아이디</label>
                <input type='text' name="id" value="<%= id %>"/>
            </div>
            <div>
                <label>비밀번호</label>
                <input type='text' name="password" value="<%= password %>"/>
            </div>
            <div>
                <label>이름</label>
                <input type='name' name="name" value="<%= name %>"/>
            </div>
            <div>
                <label>소속 부서</label>
                <input type='text' name="belong" value="<%= belong %>"/>
            </div>
            <div>
                <label>연락처</label>
                <input type='text' name="contact" value="<%= contact %>"/>
            </div>
            <div>
                <label>주소</label>
                <input type='text' name="address" value="<%= address %>"/>
            </div>
            <div>
                <label>생일</label>
                <input type='date' name="birth" value="<%= birth %>"/>
            </div>
            <input type="submit" value="수정하기"/>
       </form>  
</body> 
</html>
